let S={token:localStorage.lm_token,user:JSON.parse(localStorage.lm_user||"null"),profile:JSON.parse(localStorage.lm_profile||"null"),sessionId:localStorage.lm_session};let sock,quizData;const $=x=>document.getElementById(x);function show(x){document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));$(x).classList.add('active')}function tab(r){$('role').value=r;$('kidFields').classList.toggle('hidden',r!=='kid');$('adultFields').classList.toggle('hidden',r==='kid');if(r==='parent'){$('email').value='parent@demo.com';$('password').value='demo123'}if(r==='teacher'){$('email').value='teacher@demo.com';$('password').value='demo123'}}async function api(p,o={}){o.headers={...(o.headers||{}),'Content-Type':'application/json',...(S.token?{Authorization:'Bearer '+S.token}:{})};let r=await fetch(p,o),d=await r.json();if(!r.ok)throw Error(d.error||'Error');return d}async function login(e){e.preventDefault();let r=$('role').value,b=r==='kid'?{role:r,studentCode:$('studentCode').value,pin:$('pin').value}:{role:r,email:$('email').value,password:$('password').value};try{let d=await api('/api/auth/login',{method:'POST',body:JSON.stringify(b)});S={token:d.token,user:d.user,profile:d.profile,sessionId:d.sessionId};localStorage.lm_token=d.token;localStorage.lm_user=JSON.stringify(d.user);if(d.profile)localStorage.lm_profile=JSON.stringify(d.profile);if(d.sessionId)localStorage.lm_session=d.sessionId;route()}catch(e){$('msg').textContent=e.message}}async function logout(){try{if(S.user?.role==='kid'&&S.sessionId)await api('/api/auth/logout',{method:'POST',body:JSON.stringify({sessionId:+S.sessionId})})}catch{}localStorage.clear();S={};show('login')}function connect(){if(!S.user||sock)return;sock=io();sock.on('connect',()=>sock.emit('identify',{userId:S.user.id}));sock.on('notification',n=>{let e=S.user.role==='parent'?$('liveParent'):$('liveTeacher');e.style.display='block';e.textContent=n.message;if(S.user.role==='parent')loadNotifs()})}async function route(){if(!S.user)return show('login');show(S.user.role);connect();if(S.user.role==='kid')loadKid();if(S.user.role==='parent')loadParent();if(S.user.role==='teacher')loadTeacher()}async function loadKid(){let d=await api('/api/kid/today');S.profile=d.profile;$('kidHi').textContent='Hi '+S.user.full_name+' 👋';$('today').innerHTML='<h3>Today’s Study</h3>'+(d.assignments.length?d.assignments.map(a=>`<div class="row"><div><b>${a.subject}: ${a.topic}</b><small>${a.instructions||''}</small></div><span class="pill">Teacher Set</span></div>`).join(''):'<p>No teacher topic set for today.</p>');$('quizSubject').innerHTML=(d.profile.subjects||[]).map(x=>`<option>${x}</option>`).join('')}function panel(id){['quiz','tutor','practice'].forEach(x=>$(x).classList.add('hidden'));$(id).classList.remove('hidden')}async function startQuiz(){quizData=await api('/api/kid/quiz/start',{method:'POST',body:JSON.stringify({subject:$('quizSubject').value})});$('quizArea').innerHTML=quizData.questions.length?quizData.questions.map((q,i)=>`<div class="question"><b>${i+1}. ${q.question}</b><div class="opts">${q.options.map((o,j)=>`<label><input type="radio" name="q${q.id}" value="${j}">${o}</label>`).join('')}</div></div>`).join('')+'<button onclick="submitQuiz()">Submit Quiz</button>':'<p>No matching questions in question bank yet.</p>'}async function submitQuiz(){let answers=quizData.questions.map(q=>{let x=document.querySelector(`input[name=q${q.id}]:checked`);return{questionId:q.id,selectedIndex:x?+x.value:-1}}),d=await api('/api/kid/quiz/submit',{method:'POST',body:JSON.stringify({subject:quizData.subject,topic:quizData.topic,answers})});$('quizArea').innerHTML=`<h3>Result: ${d.attempt.score}/${d.attempt.total} — ${d.attempt.percent}%</h3>`+d.detail.map(x=>`<div class="question"><b>${x.correct?'✅':'❌'} ${x.question}</b><p>${x.explanation||''}</p></div>`).join('')}async function askAI(){let m=$('ask').value.trim();if(!m)return;bubble(m,1);$('ask').value='';let d=await api('/api/tutor',{method:'POST',body:JSON.stringify({message:m,subject:$('quizSubject').value})});bubble(d.answer,0)}function bubble(t,me){let x=document.createElement('div');x.className='bubble'+(me?' me':'');x.textContent=t;$('chat').appendChild(x)}async function loadNotifs(){let d=await api('/api/notifications');$('notifs').innerHTML=d.slice(0,12).map(n=>`<div class="row"><div><b>${n.kid_name||'Student'}</b><small>${n.message}</small></div><small>${new Date(n.created_at).toLocaleString()}</small></div>`).join('')}async function loadParent(){let k=await api('/api/parent/kids');$('parentKids').innerHTML=k.map(x=>`<div class="row"><div><b>${x.full_name}</b><small>${x.student_code} • Class ${x.standard} • ${x.board}</small></div><button onclick="report(${x.user_id||x.id},'parentReport')">Report</button></div>`).join('');loadNotifs();if(k[0])report(k[0].user_id||k[0].id,'parentReport')}async function loadTeacher(){let k=await api('/api/teacher/students');$('teacherKids').innerHTML=k.map(x=>`<div class="row"><div><b>${x.full_name}</b><small>${x.student_code} • Class ${x.standard} • ${x.board}</small></div><button onclick="report(${x.id},'teacherReport')">Report</button></div>`).join('');if(k[0])report(k[0].id,'teacherReport')}async function report(id,target){let d=await api('/api/report/'+id),w=d.subjectSummary[0];$(target).innerHTML=`<h3>${d.profile.full_name} — Complete Study Report</h3><div class="metrics"><div class="metric"><b>${Math.round(d.studySeconds/60)} min</b>Total study time</div><div class="metric"><b>${d.quizzes.length}</b>Quiz attempts</div><div class="metric"><b>${d.activities.length}</b>Tracked activities</div><div class="metric"><b>${w?w.avg_percent+'%':'—'}</b>${w?w.subject+' lowest':'No data'}</div></div><h4>Subject Performance</h4>`+(d.subjectSummary.length?d.subjectSummary.map(s=>`<div class="row"><b>${s.subject}</b><span>${s.avg_percent}% • ${s.attempts} quiz(es)</span></div>`).join(''):'<p>No quiz data yet.</p>')+'<h4>Smart Suggestions</h4>'+d.suggestions.map(s=>`<div class="row"><div><b>${s.subject} — ${s.level}</b><small>${s.text}</small></div></div>`).join('')+'<h4>Recent Activity</h4>'+d.activities.slice(0,10).map(a=>`<div class="row"><div><b>${a.title||a.activity_type}</b><small>${a.subject||''} • ${a.activity_type}</small></div><small>${new Date(a.created_at).toLocaleString()}</small></div>`).join('')}async function assign(e){e.preventDefault();await api('/api/teacher/assignment',{method:'POST',body:JSON.stringify({board:$('ab').value,standard:$('as').value,subject:$('asu').value,topic:$('at').value,instructions:$('ai').value})});alert('Today’s syllabus assigned. Matching students will see it now.')}route();